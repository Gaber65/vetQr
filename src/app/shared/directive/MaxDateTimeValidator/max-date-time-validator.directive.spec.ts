import { MaxDateTimeValidatorDirective } from './max-date-time-validator.directive';

describe('MaxDateTimeValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new MaxDateTimeValidatorDirective();
    expect(directive).toBeTruthy();
  });
});

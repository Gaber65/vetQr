import { ToUniversalDateTimeDirective } from './to-universal-date-time.directive';

describe('ToUniversalDateTimeDirective', () => {
  it('should create an instance', () => {
    const directive = new ToUniversalDateTimeDirective();
    expect(directive).toBeTruthy();
  });
});

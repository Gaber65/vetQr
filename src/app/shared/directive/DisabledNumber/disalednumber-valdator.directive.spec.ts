import { DisalednumberValdatorDirective } from './disalednumber-valdator.directive';

describe('DisalednumberValdatorDirective', () => {
  it('should create an instance', () => {
    const directive = new DisalednumberValdatorDirective();
    expect(directive).toBeTruthy();
  });
});

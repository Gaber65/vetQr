import { MinDateTeimValidatorDirective } from './min-date-teim-validator.directive';

describe('MinDateTeimValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new MinDateTeimValidatorDirective();
    expect(directive).toBeTruthy();
  });
});

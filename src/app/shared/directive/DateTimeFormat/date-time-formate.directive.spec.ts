import { DateTimeFormateDirective } from './date-time-formate.directive';

describe('DateTimeFormateDirective', () => {
  it('should create an instance', () => {
    const directive = new DateTimeFormateDirective();
    expect(directive).toBeTruthy();
  });
});
